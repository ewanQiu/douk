# douk 使用说明

批量下载 TikTok 短剧 / 合集 / 主页作品。

> 本文只讲怎么用，面向不熟悉命令行的读者。
> 技术原理、接口分析、踩坑记录见 [README.zh-CN.md](README.zh-CN.md)。
> English readers: see [README.md](README.md), which is self-contained.

---

## 一、首次配置（只做一次）

### 1. 安装

双击 `setup.bat`，或在终端里：

```powershell
.\setup.bat
```

它会自动找 Python（需 3.11+，没有就用 winget 装）、建虚拟环境、装依赖、
检查 Chrome 和 ffmpeg，最后跑一次自检。

> 别直接跑 `setup.ps1` —— Windows 默认禁止运行 `.ps1`，会报
> 「因为在此系统上禁止运行脚本」。`.bat` 不受此限制。
> 非要用 ps1 的话：`powershell -ExecutionPolicy Bypass -File .\setup.ps1`

### 2. 拿登录态（关键，不做下不了任何东西）

TikTok 不登录就不返回播放地址，所以这步跳不过。

最终只需要产出一个文件：**`data\cookies.txt`**。下面两种办法任选，
第一种更省事。

---

#### 办法一：用 Get cookies.txt LOCALLY 扩展（推荐）

**安装扩展**

在 Chrome 里打开 [Chrome 应用商店](https://chromewebstore.google.com)，
搜索 `Get cookies.txt LOCALLY`，点「添加至 Chrome」。

这个扩展是开源的，只在本地导出、不上传任何数据。如果商店搜不到或装不了
（这类扩展偶尔会被下架），从 GitHub 装：

1. 打开 `github.com/kairi003/Get-cookies.txt-LOCALLY`，在 Releases 里下载 zip 并解压
2. Chrome 地址栏输入 `chrome://extensions/`
3. 打开右上角的**开发者模式**
4. 点「加载已解压的扩展程序」，选中刚解压的文件夹

**导出 cookie**

1. 在 Chrome 里打开 **tiktok.com 并确保已登录**
2. 点浏览器右上角的扩展图标，选 Get cookies.txt LOCALLY
3. 确认 **Export Format 是 `Netscape`**（默认就是）
4. 点左边的 **Export** 按钮

> ⚠️ 点 **Export**，不是 `Export All Cookies`。后者会导出你**所有网站**的
> cookie，没必要也不安全。

5. 浏览器会下载一个类似 `www.tiktok.com_cookies.txt` 的文件
6. 把它移动到 `d:\douk\data\` 并**重命名为 `cookies.txt`**

命令行搬运的话：

```bash
cd D:\douk
Copy-Item "$env:USERPROFILE\Downloads\*tiktok*cookies*.txt" data\cookies.txt
```

---

#### 办法二：从 DevTools 抄 Cookie 请求头（不装扩展）

Network 面板里请求头的 `Cookie:` 字段包含**全部** cookie，HttpOnly 的也在内。

1. 在已登录 TikTok 的 Chrome 里打开 tiktok.com
2. 按 `F12` → 切到 **Network** 面板 → 刷新页面
3. 点列表里任意一个发往 `tiktok.com` 的请求
4. 右侧 **Headers** → 往下找 **Request Headers** → 找到 `Cookie:` 那一行
5. 右键 → **Copy value**
6. 粘贴到 `d:\douk\data\header.txt` 保存
7. 转换成 cookies.txt：

```bash
.\douk cookies --from-header data\header.txt
```

> ⚠️ 必须是 **Request Headers** 里的 `Cookie:`，不是 Response Headers 里的
> `Set-Cookie:`。抄错了转换会直接报错提醒你。

---

#### 验证（两种办法都要做）

```bash
.\douk verify
```

看到 `验证通过：TikTok 认出的账号是 xxx` 就成了。

如果报「里面没有 sessionid」，说明导出时漏掉了 HttpOnly cookie —— 用办法一
重新导一次，或改用办法二。

> **Cookie 是唯一需要维护的凭证。** 大概几周到几个月失效，届时 `verify`
> 会报错，重做本节即可。`data\browser\` 只是运行缓存，删掉不影响功能。

> ⚠️ `cookies.txt` 等同于你的 TikTok 账号登录凭证，别发给别人、别传到公开仓库。

---

## 二、日常使用

### 嫌命令长？

**最省事：用自带的 `douk.cmd`**，什么都不用设置，cmd 和 PowerShell 都认：

```powershell
cd D:\douk
.\douk verify
.\douk sync "https://www.tiktok.com/@user/video/123"
```

> 注意 PowerShell 里要写 `.\douk`，光写 `douk` 会找不到（PowerShell 出于安全
> 不搜索当前目录）。

**PowerShell 里想直接敲 `douk`**，定义一个函数（`alias` 是 bash 语法，
PowerShell 里 `alias` 只是 `Get-Alias`、只能读不能写；`Set-Alias` 也不行，
它只能指向一个可执行文件、不能带参数）：

```powershell
function douk { & "D:\douk\.venv\Scripts\python.exe" "D:\douk\douk.py" @args }
```

`@args` 负责把参数原样透传。想永久生效就写进 PowerShell 配置文件：

```powershell
if (-not (Test-Path $PROFILE)) { New-Item -ItemType File -Path $PROFILE -Force }
Add-Content $PROFILE 'function douk { & "D:\douk\.venv\Scripts\python.exe" "D:\douk\douk.py" @args }'
. $PROFILE     # 让当前窗口立刻生效
```

**Git Bash / WSL** 里才是 `alias`：

```bash
alias douk='/d/douk/.venv/Scripts/python.exe /d/douk/douk.py'
```

下面的例子都用 `douk` 代表上面任意一种写法。把 `D:\douk` 换成你自己的实际路径。

### 看一个号有哪些剧

```bash
douk dramas "https://www.tiktok.com/@某账号"
```

输出每部剧的 ID、集数、剧名。

### 下载一整部剧

复制该剧**任意一集**的视频链接：

```bash
douk sync "https://www.tiktok.com/@某账号/video/1234567890"
```

它会自动：识别这条视频属于哪部剧 → 采集全部集数 → 逐集下载。

### 下载整个号的作品

```bash
douk sync "https://www.tiktok.com/@某账号"
```

### 查看进度

```bash
douk status
```

---

## 三、常用参数

### 只想先试几集

```bash
douk sync "<链接>" --collect-only     # 只采清单，不下载
douk download -t <剧ID> --limit 3     # 只下 3 集试试
```

### 选择画质与编码

```bash
douk sync "<链接>" --codec h264       # 默认。576x1024，到处能播
douk sync "<链接>" --codec h265       # 1080p，但播放器要支持 H.265
douk sync "<链接>" --codec transcode  # 1080p 再转 H.264，慢且占空间
```

**怎么选：**

| 你的情况 | 选 |
|---|---|
| 随便看看，能播就行 | `h264`（默认） |
| 要 1080p，且装了 VLC / PotPlayer / MPV | `h265` |
| 要 1080p，又只想用系统自带播放器 | `transcode` |

细节见下方「关于画质」。

### 看着浏览器跑（调试 / 手动过验证码）

```bash
douk sync "<链接>" --headful
```

---

## 四、断点与增量

**中断了直接重跑，不会重复下载。**

```bash
douk sync "<之前的链接>"      # 已下好的自动跳过，只补缺的
douk download -t <剧ID>       # 只重试失败的
```

剧集更新了也是同一条命令，只会下新增的那几集。

---

## 五、文件放在哪

```
d:\douk\data\
├── videos\剧名\
│   ├── 0001_视频ID_剧名.mp4      ← 视频，前 4 位是集号
│   ├── 0001_视频ID_剧名.srt      ← 字幕
│   ├── 0001_视频ID_剧名.jpg      ← 封面
│   └── 0001_视频ID_剧名.info.json ← 元数据
├── douk.db          ← 数据库，记录下载进度
├── cookies.txt      ← 登录态
└── browser\         ← 浏览器数据
```

文件名开头的 `0001`、`0002` 就是集号，按顺序排好的，直接按文件名排序播放即可。

---

## 六、关于画质

TikTok 对同一个视频提供两套编码，**必须二选一**：

| 编码 | 最高分辨率 | 能不能直接播 |
|---|---|---|
| H.264 | 576×1024 | ✅ 到处都能播 |
| H.265 | **1080×1920** | ❌ Windows 自带播放器要装付费扩展 |

**想要 1080p 就绕不开 H.265。** 三个办法：

1. **装个播放器**（最推荐）—— VLC、PotPlayer、MPV 都原生支持 H.265，
   免费、不用转码、不损画质。装完用 `--codec h265` 即可。
2. **用 `--codec h264`**（默认）—— 分辨率降到 576×1024，手机上看其实够用。
3. **用 `--codec transcode`** —— 下 1080p 再转成 H.264。画质兼容都要，
   代价是每集多花约 23 秒 CPU，**文件会变大约 2.3 倍**
   （H.264 编码效率不如 H.265，这是格式本身决定的，无法避免）。

### 已经下好的想转格式

不用重下：

```bash
douk convert -t <剧ID>
```

只转 H.265 的文件，已经是 H.264 的自动跳过。中断了再跑一次会接着做。

---

## 七、遇到问题

| 现象 | 怎么办 |
|---|---|
| 提示 cookie 过期 / 采不到东西 | 重做「一、2 拿登录态」 |
| 视频双击打不开、提示缺编解码器 | 装 VLC，或跑 `douk convert -t <剧ID>` |
| 下载很慢 / 感觉被限速 | 改 `config.toml`：`concurrency` 调小，`delay_min/max` 调大 |
| 提示命中验证码 | 加 `--headful` 手动过一次，或换个网络 |
| `dramas` 说没找到短剧 | 这个号可能用的是合集或普通作品，直接 `douk sync "<主页链接>"` |
| 提示「只采到 N/M 集，没采全」 | 网络慢，分页没加载完。改 `config.toml` 把 `scroll_wait_ms` 调大（如 2500）再跑 |
| 集数比预期多一倍 | 作者把整部剧重新上传过一遍，TikTok 两份都算。见下方说明 |
| 不确定环境有没有问题 | 跑 `douk doctor` 体检 |

### 完整命令列表

```bash
douk --help          # 看所有命令
douk <命令> --help   # 看某个命令的参数
```

| 命令 | 作用 |
|---|---|
| `cookies --from-header <文件>` | 从 Cookie 请求头生成登录态 |
| `verify` | 验证登录态是否可用 |
| `dramas <主页链接>` | 列出账号下所有短剧 |
| `resolve <链接>` | 看看这条链接会被识别成什么（不下载） |
| `sync <链接>` | 一条龙：识别 → 采集 → 下载 |
| `collect <链接>` | 只采清单不下载 |
| `download -t <剧ID>` | 下载 / 重试 |
| `convert -t <剧ID>` | 已下文件转 H.264 |
| `status` | 查看进度 |
| `doctor` | 环境体检 |

---

## 八、关于「集数翻倍」

有些作者会把整部剧**重新上传一遍**，TikTok 会把两份都算进集数里。

实测某部标称 80 集的剧：解析集号后只有 **40 个不同的集号，每个都出现两次**，
两条的时长完全一样，但视频 ID 不同、发布时间差了约 6 天 —— 是作者隔了几天
重新传了一遍全剧。

douk 按 TikTok 的说法如实抓 80 条，不会自作主张删掉一半（有的"重复"其实是
不同剪辑版本，误删了更麻烦）。下完之后你会看到成对的文件，自己挑一份留即可。

判断方法：文件名里的视频 ID 不同、但集号标题相同的，就是同一集的两个版本。

## 九、搬到另一台电脑

### 在原电脑上打包

```powershell
.\package.ps1
```

生成 `douk-package.zip`，约 **0.05 MB**（只有源码和配置）。

**为什么这么小**：`.venv`（164 MB）和 `data\browser`（231 MB）都被排除了。
venv 里写死了本机绝对路径，拷过去根本用不了，必须在新机器重建；
浏览器 profile 是本机状态，同样没有拷贝价值。

想连登录态和下载记录一起带走：

```powershell
.\package.ps1 -IncludeCookies -IncludeDb
```

> ⚠️ `-IncludeCookies` 会把 `cookies.txt` 打进包里，**那等同于你的 TikTok 账号凭证**。
> 只在拷给自己时用，别发给别人。

### 在新电脑上落地

1. 解压到任意目录
2. 在该目录执行：

```powershell
.\setup.ps1
```

它会自动：找 Python（需 3.11+，用到 `tomllib`）→ 建 venv → 装依赖 →
检查 Chrome 和 ffmpeg → 跑一次自检。

没装 Chrome 的话加 `-Chromium` 用 Playwright 自带浏览器（约 300 MB），
同时把 `config.toml` 里 `channel` 改成 `""`：

```powershell
.\setup.ps1 -Chromium
```

3. 改 `config.toml` 里的 `out_dir` —— 里面还是上一台机器的路径
4. 没带 cookie 的话，按本文第一节重新导出一次
5. 验证：

```powershell
.\.venv\Scripts\python.exe douk.py doctor
.\.venv\Scripts\python.exe douk.py verify
```

### 新机器需要具备

| 依赖 | 必需？ | 说明 |
|---|---|---|
| Python 3.11+ | ✅ | 低于 3.11 没有 `tomllib`，读不了配置 |
| Chrome | ✅ | 或改用 Playwright 自带浏览器 |
| ffmpeg | ⭕ | 只影响转码和封面转换，不装也能下载 |
| 能访问 TikTok 的网络 | ✅ | 见下方说明，douk 本身不提供翻墙能力 |

`doctor` 会把上面每一项都检一遍，缺什么直接报出来。

### 关于网络

**douk 不提供任何翻墙能力**，它只能用你本机已有的网络环境。TikTok 在大陆
直连不通，得先自己解决这件事。

能访问之后，`proxy` 填什么取决于**流量怎么走**，而不是你用哪个软件
（同一个 Clash，开 TUN 模式属第一行，只开端口属第二行）：

| 流量走法 | `proxy` 怎么填 |
|---|---|
| 透明转发（VPN 的 TUN 模式、WARP、路由器分流） | **留空** |
| 本地监听端口（客户端界面上写着 7890 之类） | 填那个地址，如 `http://127.0.0.1:7890` |
| 勾了「设为系统代理」 | **留空**，douk 会自己读系统设置 |

> 常见误解：以为在 `proxy` 里随便填个地址就能访问。**这一项不会凭空创造连通性**
> —— 它只是告诉 douk「去用一个已经在跑的代理」。没有那个东西在跑，填什么都是
> 连接被拒。端口号也要以你客户端的实际设置为准，别照抄 7890。

留空不等于直连：会依次回退到环境变量和系统代理设置。拿不准就留空跑一次：

```powershell
.\douk doctor
```

它会直接告诉你当前配置下 TikTok 通不通。

## 十、注意

- 仅供个人离线观看与归档，视频版权归原作者所有，**请勿二次分发或商用**。
- 别把 `concurrency` 调太大、`delay` 调太小，高频抓取会触发平台风控。
- `data/cookies.txt` 和 `data/header.txt` 等同于你的账号登录凭证，**别外传**，
  也别提交到版本库（`.gitignore` 已默认排除）。

完整条款见 [DISCLAIMER.md](DISCLAIMER.md)，源代码许可见 [LICENSE](LICENSE)。
